import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  heart: {
    marginHorizontal: 5,
  },
  lives: {
    color: 'red',
    fontWeight: 'bold',
    fontSize: 15,
  },
})

export default styles
