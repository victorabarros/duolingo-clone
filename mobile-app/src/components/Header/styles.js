import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    marginVertical: 5,
  },
  heart: {
    marginHorizontal: 5,
  },
  lives: {
    color: "red",
    fontWeight: "bold",
    fontSize: 15,
  },
})

export default styles
